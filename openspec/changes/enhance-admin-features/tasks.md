# 任务列表：完善后台管理功能

## 概述

本文档列出了实现"完善后台管理功能"所需的所有任务。任务按功能模块分组，每个任务都有明确的验收标准。

## 任务状态说明

- ⬜ 待开始
- 🔄 进行中
- ✅ 已完成
- ❌ 已取消

---

## 第一阶段：文章标签选择 + 分类标签编辑

### 模块 1：文章标签选择功能

#### 任务 1.1：创建 TagSelector 组件 ✅

**描述**：创建一个可复用的标签选择组件，支持多选、搜索和创建新标签。

**文件**：
- `src/components/admin/TagSelector.tsx`（新建）

**验收标准**：
- [ ] 组件支持多选标签
- [ ] 支持搜索标签功能
- [ ] 支持创建新标签
- [ ] 显示已选标签数量
- [ ] 支持键盘操作（上下键、回车）
- [ ] 使用 Radix UI Select 或类似组件
- [ ] 类型安全，使用 TypeScript

**依赖**：无

---

#### 任务 1.2：更新文章编辑页面集成标签选择 ✅

**描述**：在文章创建/编辑页面中集成 TagSelector 组件。

**文件**：
- `src/app/admin/(dashboard)/posts/new/page.tsx`

**验收标准**：
- [ ] 文章编辑页面显示 TagSelector 组件
- [ ] 编辑模式下预填充已选标签
- [ ] 保存文章时包含标签数据
- [ ] 表单验证包含标签字段
- [ ] UI 与现有风格一致

**依赖**：任务 1.1

---

#### 任务 1.3：实现文章标签关联 Server Action ✅

**描述**：创建或更新 Server Action 以处理文章和标签的关联。

**文件**：
- `src/server/actions/posts.ts`

**验收标准**：
- [ ] 创建 `updatePostTags` 函数
- [ ] 使用 Prisma 事务确保数据一致性
- [ ] 删除旧的标签关联
- [ ] 创建新的标签关联
- [ ] 更新标签的 postCount 字段
- [ ] 错误处理和返回类型安全

**依赖**：无

---

#### 任务 1.4：更新文章列表显示标签 ✅

**描述**：在文章列表页面显示文章关联的标签。

**文件**：
- `src/app/admin/(dashboard)/posts/page.tsx`

**验收标准**：
- [ ] 文章列表查询包含标签数据
- [ ] 显示文章的标签（最多 3 个）
- [ ] 标签样式美观，使用徽章组件
- [ ] 超过 3 个标签显示 "+N" 提示

**依赖**：任务 1.3

---

### 模块 2：分类编辑功能

#### 任务 2.1：创建分类编辑页面 ✅

**描述**：创建分类编辑页面，允许修改分类信息。

**文件**：
- `src/app/admin/(dashboard)/categories/edit/[id]/page.tsx`（新建）

**验收标准**：
- [ ] 页面加载现有分类数据
- [ ] 复用 CategoryForm 组件
- [ ] 预填充表单字段
- [ ] 显示使用该分类的文章数量
- [ ] 保存按钮触发更新操作
- [ ] 成功后跳转回列表页面

**依赖**：无

---

#### 任务 2.2：更新 CategoryForm 组件支持编辑模式 ✅

**描述**：修改 CategoryForm 组件以支持创建和编辑两种模式。

**文件**：
- `src/app/admin/(dashboard)/categories/_components/CategoryForm.tsx`

**验收标准**：
- [ ] 组件接受 `initialData` 参数
- [ ] 编辑模式下预填充数据
- [ ] 表单提交调用不同的 Server Action
- [ ] 按钮文本根据模式变化（创建/更新）
- [ ] 验证逻辑保持一致

**依赖**：无

---

#### 任务 2.3：实现分类更新 Server Action ✅

**描述**：创建或更新 Server Action 以处理分类更新。

**文件**：
- `src/server/actions/categories.ts`

**验收标准**：
- [ ] 创建 `updateCategory` 函数
- [ ] 验证分类 ID 存在
- [ ] 使用 Zod 验证输入数据
- [ ] 检查 slug 唯一性（排除当前分类）
- [ ] 更新分类记录
- [ ] 返回成功/失败结果
- [ ] 错误处理完善

**依赖**：无

---

#### 任务 2.4：在分类列表添加编辑按钮 ✅

**描述**：在分类列表页面为每个分类添加编辑按钮。

**文件**：
- `src/app/admin/(dashboard)/categories/page.tsx`

**验收标准**：
- [ ] 每个分类行显示编辑按钮
- [ ] 点击编辑按钮跳转到编辑页面
- [ ] 按钮样式与现有风格一致
- [ ] 使用 Link 组件实现导航

**依赖**：任务 2.1

---

### 模块 3：标签编辑功能

#### 任务 3.1：创建标签编辑页面 ✅

**描述**：创建标签编辑页面，允许修改标签信息。

**文件**：
- `src/app/admin/(dashboard)/tags/edit/[id]/page.tsx`（新建）

**验收标准**：
- [ ] 页面加载现有标签数据
- [ ] 复用 TagForm 组件
- [ ] 预填充表单字段
- [ ] 显示使用该标签的文章和相册数量
- [ ] 保存按钮触发更新操作
- [ ] 成功后跳转回列表页面

**依赖**：无

---

#### 任务 3.2：更新 TagForm 组件支持编辑模式 ✅

**描述**：修改 TagForm 组件以支持创建和编辑两种模式。

**文件**：
- `src/app/admin/(dashboard)/tags/_components/TagForm.tsx`

**验收标准**：
- [ ] 组件接受 `initialData` 参数
- [ ] 编辑模式下预填充数据
- [ ] 表单提交调用不同的 Server Action
- [ ] 按钮文本根据模式变化（创建/更新）
- [ ] 验证逻辑保持一致

**依赖**：无

---

#### 任务 3.3：实现标签更新 Server Action ✅

**描述**：创建或更新 Server Action 以处理标签更新。

**文件**：
- `src/server/actions/tags.ts`

**验收标准**：
- [ ] 创建 `updateTag` 函数
- [ ] 验证标签 ID 存在
- [ ] 使用 Zod 验证输入数据
- [ ] 检查 slug 唯一性（排除当前标签）
- [ ] 更新标签记录
- [ ] 返回成功/失败结果
- [ ] 错误处理完善

**依赖**：无

---

#### 任务 3.4：在标签列表添加编辑按钮 ✅

**描述**：在标签列表页面为每个标签添加编辑按钮。

**文件**：
- `src/app/admin/(dashboard)/tags/page.tsx`

**验收标准**：
- [ ] 每个标签行显示编辑按钮
- [ ] 点击编辑按钮跳转到编辑页面
- [ ] 按钮样式与现有风格一致
- [ ] 使用 Link 组件实现导航

**依赖**：任务 3.1

---

## 第二阶段：相册删除 + 批量操作

### 模块 4：相册删除功能

#### 任务 4.1：创建 DeleteGalleryButton 组件 ✅

**描述**：创建相册删除按钮组件，包含确认对话框。

**文件**：
- `src/app/admin/(dashboard)/galleries/_components/DeleteGalleryButton.tsx`（新建）

**验收标准**：
- [ ] 组件显示删除按钮
- [ ] 点击按钮显示确认对话框
- [ ] 对话框显示相册信息（标题、图片数量）
- [ ] 警告删除操作不可逆
- [ ] 确认后调用删除 Server Action
- [ ] 显示删除进度（loading 状态）
- [ ] 使用 Toast 显示成功/失败消息

**依赖**：无

---

#### 任务 4.2：实现相册删除 Server Action ✅

**描述**：创建或更新 Server Action 以处理相册删除。

**文件**：
- `src/server/actions/galleries.ts`

**验收标准**：
- [ ] 创建 `deleteGallery` 函数
- [ ] 验证相册 ID 存在
- [ ] 使用 Prisma 事务删除：
  - [ ] 删除 PhotoImage 记录
  - [ ] 删除 GalleryTag 关联
  - [ ] 删除 PhotoGallery 记录
- [ ] 返回成功/失败结果
- [ ] 错误处理完善

**依赖**：无

---

#### 任务 4.3：在相册列表添加删除按钮 ✅

**描述**：在相册列表页面为每个相册添加删除按钮。

**文件**：
- `src/app/admin/(dashboard)/galleries/page.tsx`

**验收标准**：
- [ ] 每个相册行显示删除按钮
- [ ] 使用 DeleteGalleryButton 组件
- [ ] 删除成功后刷新列表
- [ ] 按钮样式与现有风格一致

**依赖**：任务 4.1, 4.2

---

### 模块 5：批量操作功能

#### 任务 5.1：创建 BatchActions 组件 ✅

**描述**：创建通用的批量操作组件。

**文件**：
- `src/components/admin/BatchActions.tsx`（新建）

**验收标准**：
- [ ] 组件支持全选/取消全选
- [ ] 显示已选数量
- [ ] 提供批量删除按钮
- [ ] 提供批量修改状态按钮（可选）
- [ ] 操作前显示确认对话框
- [ ] 使用 Toast 显示操作结果
- [ ] 组件可复用，支持不同实体类型

**依赖**：无

---

#### 任务 5.2：在文章列表集成批量操作 ✅

**描述**：在文章列表页面集成批量操作功能。

**文件**：
- `src/app/admin/(dashboard)/posts/page.tsx`

**验收标准**：
- [ ] 每行显示复选框
- [ ] 表头显示全选复选框
- [ ] 集成 BatchActions 组件
- [ ] 支持批量删除文章
- [ ] 支持批量修改文章状态
- [ ] 操作成功后刷新列表

**依赖**：任务 5.1

---

#### 任务 5.3：实现文章批量操作 Server Actions ✅

**描述**：创建 Server Actions 处理文章批量操作。

**文件**：
- `src/server/actions/posts.ts`

**验收标准**：
- [ ] 创建 `batchDeletePosts` 函数
- [ ] 创建 `batchUpdatePostStatus` 函数
- [ ] 使用 Prisma 事务确保原子性
- [ ] 限制单次操作数量（最多 100 条）
- [ ] 返回操作结果（成功数量、失败数量）
- [ ] 错误处理完善

**依赖**：无

---

## 任务统计

**总任务数**：17
**已完成**：17
**进行中**：0
**待开始**：0

---

## 下一步

完成第一阶段和第二阶段后，继续实现：
- 第三阶段：搜索筛选 + 仪表板增强

详细任务将在后续补充。
